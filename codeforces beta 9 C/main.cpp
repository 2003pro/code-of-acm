#include <bits/stdc++.h>

using namespace std;
string s;
vectorl..<int> v;
int main()
{
    cin>>s;
    int l = s.size();
    for(int i = 0;i < l;i++){
        if(s[i] != '1' && s[i] != '0') v.push_back()
    }
    return 0;
}
