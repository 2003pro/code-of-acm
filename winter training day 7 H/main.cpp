#include <bits/stdc++.h>
#define rep(i,j,k) for(int i = (int)j;i < (int)k;i++)
using namespace std;
const int N = 1e5+3;
int a[N];
int main()
{
    int T;scanf("%d",&T);
    while(T--){
        int s,n;scanf("%d%d",&s,&n);
        rep(i,0,n) scanf("%d",&a[i]);

    }
    return 0;
}
